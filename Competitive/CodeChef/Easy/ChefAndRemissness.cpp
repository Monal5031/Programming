# include "bits/stdc++.h"

    using namespace std

    int main() {
        ios: : sync_with_stdio(false);
        int tc;
        cin >> tc;
        while (tc--) {
            long int a, b;
            cin >> a >> b;
            cout<<max(a, b)<<" "<<a+b<<"\n"; }
        return 0;}
