t=int(input())
for k in range(0,t):
	n=int(input())
	arr=[int(y) for y in raw_input().split()]
	arr.sort()
	min=arr[1]-arr[0];
	for i in range(2,n):
		if(arr[i]-arr[i-1]<min):
			min=arr[i]-arr[i-1];
print min
